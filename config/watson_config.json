{
    "nlc" : {
        // REQUIRED whether running locally or not
        // "classifier_name" : "CLASSIFIER NAME",
        "classifier_name" : "nlc_factoid",

        // OPTIONAL: Used when running the app locally and NOT on BLuemix
        "username": "LOCAL NLC SERVICE USERNAME",
        "password": "LOCAL NLC SERVICE PASSWORD"
    },
    "alchemyapi" : {
        // OPTIONAL: Used when running the app locally and NOT on BLuemix
        "api_key": "LOCAL ALCHEMY API KEY"
    },
    "cloudant" : {
        // REQUIRED IF AND ONLY IF you want to store your user submitted questions for later analysis
        "db_name": "Remote Database Name",

        // OPTIONAL: Used when running the app locally and NOT on BLuemix
        "url": "Remote Database URL",
        "username": "LOCAL NLC SERVICE USERNAME",
        "password": "LOCAL NLC SERVICE PASSWORD"
    }
}
